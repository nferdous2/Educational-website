import React from 'react';
import notfound from '../../image/giphy.gif'
const NotFound = () => {
    return (
        <div>
            <img style={{ width: '50%' }} src={notfound} alt="" />
        </div>

    );
};

export default NotFound;